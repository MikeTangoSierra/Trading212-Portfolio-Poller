apiVersion: v1
kind: Service
metadata:
  name: {{ include "trading212poller.fullname" . }}
  labels:
    {{- include "trading212poller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trading212poller.selectorLabels" . | nindent 4 }}
