apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Trading212PortfolioPoller.fullname" . }}-test-connection"
  labels:
    {{- include "Trading212PortfolioPoller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Trading212PortfolioPoller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
