apiVersion: v1
kind: Service
metadata:
  name: {{ include "Trading212PortfolioPoller.fullname" . }}
  labels:
    {{- include "Trading212PortfolioPoller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Trading212PortfolioPoller.selectorLabels" . | nindent 4 }}
